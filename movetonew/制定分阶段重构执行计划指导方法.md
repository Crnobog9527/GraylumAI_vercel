# 任务：制定《GraylumAI 分阶段重构执行计划》

## 项目背景

**项目名称**: GraylumAI - 社交媒体增长策略平台
**当前技术栈**: 
- 前端：Next.js 16 (App Router) + React + TypeScript
- 后端：tRPC + Drizzle ORM + Supabase
- AI：Claude API (Haiku 4.5 / Sonnet 4.5 智能路由)
- 部署：Vercel

**重构目标**: 
根据上传的 `AI_REFACTOR_DESIGN_BRIEF.md` 简报，将现有的 AI 对话系统从不稳定的 Base44 云函数迁移到新的架构，同时实现严格的成本控制和安全规范。

---

## 你的任务

**请仅制定执行计划，不要编写任何实际代码。**

你需要输出一份结构化的 Markdown 文档：《GraylumAI_分阶段重构执行计划.md》，包含以下内容：

---

## 输出文档要求

### 文档结构
```
# GraylumAI 分阶段重构执行计划

## 一、项目概览
- 重构范围总结
- 核心目标（3-5 条）
- 技术债务清单
- 风险评估

## 二、架构决策记录 (ADR)
- 为什么选择 tRPC 而非 REST API？
- 为什么使用 Drizzle ORM 而非 Prisma？
- AI 智能路由的设计原则
- 成本优化的核心策略

## 三、分阶段执行计划

### 阶段一：数据库与类型定义 (Schema & Types)
#### 交付物
- [ ] `schema/models.ts` - 完整的 AI 模型配置表
- [ ] `schema/billing_history.ts` - 计费历史表（支持退费/补偿）
- [ ] `schema/usage_logs.ts` - 使用日志表（Token 级精度）
- [ ] `types/ai.ts` - AI 请求/响应的全栈共享类型
- [ ] `types/billing.ts` - 计费相关类型定义

#### 技术要求
- 所有表必须包含 RLS 策略定义
- 必须定义 Drizzle 迁移文件
- 必须包含索引优化方案
- 类型必须支持 Zod Schema 验证

#### 验证标准
- [ ] 数据库迁移可以无错误执行
- [ ] 所有表的 RLS 策略通过测试（普通用户、管理员、匿名访问）
- [ ] TypeScript 类型无 `any` 警告
- [ ] 所有外键关系正确建立
- [ ] 索引查询性能满足要求（<100ms）

#### 手动验证清单
1. 在 Supabase Dashboard 手动执行迁移 SQL
2. 使用不同用户身份测试表的 CRUD 权限
3. 运行 `pnpm tsc --noEmit` 验证类型无错误
4. 使用 Supabase Studio 查看表关系图
5. 执行 `EXPLAIN ANALYZE` 验证索引生效

---

### 阶段二：后端核心逻辑 (tRPC Procedures)
#### 交付物
- [ ] `server/trpc/middleware/auth.ts` - protectedProcedure 中间件
- [ ] `server/trpc/middleware/admin.ts` - adminProcedure 中间件
- [ ] `server/trpc/routers/ai.ts` - AI 对话核心路由
- [ ] `server/trpc/services/billing.ts` - 原子化计费服务
- [ ] `server/trpc/services/ai-router.ts` - AI 智能路由逻辑

#### 技术要求
- 计费必须使用数据库事务（BEGIN...COMMIT）
- 必须实现预扣-结算-退费三段式逻辑
- 必须包含联网判断和模型选择算法
- 必须实现请求去重和幂等性保护

#### 验证标准
- [ ] 未登录用户无法调用 protectedProcedure
- [ ] 普通用户无法调用 adminProcedure
- [ ] 计费事务在任何异常情况下都能正确回滚
- [ ] 重复请求不会重复扣费
- [ ] AI 路由能正确判断联网需求（准确率 >95%）

#### 手动验证清单
1. 使用 Postman 测试未授权访问（应返回 401）
2. 模拟数据库连接失败，验证事务回滚
3. 使用相同 requestId 发送重复请求，验证幂等性
4. 测试 20 个不同提示词，验证路由准确性
5. 检查 Supabase 日志确认没有 RLS 绕过

---

### 阶段三：AI 引擎与成本优化 (AI Engine & Optimization)
#### 交付物
- [ ] `lib/ai/token-counter.ts` - Token 统计模块
- [ ] `lib/ai/context-compressor.ts` - 上下文滑动窗口
- [ ] `lib/ai/stream-handler.ts` - SSE 流式传输
- [ ] `lib/ai/cost-calculator.ts` - 成本计算器
- [ ] `lib/ai/cache-manager.ts` - 响应缓存系统

#### 技术要求
- Token 统计误差必须 <2%（使用 tiktoken）
- 上下文压缩必须保留关键信息（前 2 轮 + 最新 3 轮）
- SSE 必须支持中断和重连
- 缓存必须支持按用户/提示词命中

#### 验证标准
- [ ] Token 统计与 Claude API 实际计费误差 <2%
- [ ] 压缩后上下文能保持对话连贯性
- [ ] 流式输出在网络中断后能恢复
- [ ] 缓存命中率 >30%（相同提示词）
- [ ] 成本计算精确到小数点后 6 位

#### 手动验证清单
1. 对比 100 次请求的 Token 统计与 API 计费
2. 进行 50 轮对话，验证上下文压缩效果
3. 模拟网络中断，验证流式输出恢复
4. 记录 1000 次请求的缓存命中率
5. 验证成本报表与实际账单一致

---

### 阶段四：前端集成与 UI 还原 (Frontend Integration)
#### 交付物
- [ ] `components/chat/ChatInterface.tsx` - 对话界面（接入新 tRPC）
- [ ] `components/chat/MessageStream.tsx` - 流式消息渲染
- [ ] `components/chat/InterruptButton.tsx` - 中断控制
- [ ] `hooks/useAIChat.ts` - 封装 AI 对话逻辑
- [ ] `hooks/useStreamResponse.ts` - 流式响应处理

#### 技术要求
- 必须使用 tRPC React Query hooks
- 流式渲染必须逐字输出（打字机效果）
- 中断必须正确取消 API 请求
- 必须显示实时 Token 消耗和成本

#### 验证标准
- [ ] 界面像素级还原设计稿（误差 <5px）
- [ ] 流式输出延迟 <50ms（60fps）
- [ ] 中断按钮点击后 <100ms 停止
- [ ] Token/成本显示实时更新
- [ ] 移动端自适应无错位

#### 手动验证清单
1. 与设计稿逐像素对比（使用浏览器插件）
2. 录制流式输出视频，检查帧率
3. 点击中断，检查网络面板请求取消
4. 对比前端显示的 Token 与后端日志
5. 在 iPhone/Android 真机测试布局

---

### 阶段五：全链路测试与安全审计 (Testing & Security)
#### 交付物
- [ ] `__tests__/unit/billing.test.ts` - 计费逻辑单元测试
- [ ] `__tests__/unit/token-counter.test.ts` - Token 统计测试
- [ ] `__tests__/integration/ai-chat.test.ts` - 对话流程集成测试
- [ ] `__tests__/e2e/user-journey.spec.ts` - 端到端测试
- [ ] `docs/SECURITY_AUDIT.md` - 安全审计报告

#### 技术要求
- 单元测试覆盖率必须 >80%
- 必须测试并发场景（10 个用户同时请求）
- 必须测试 RLS 权限隔离
- 必须进行 SQL 注入和 XSS 测试

#### 验证标准
- [ ] 所有单元测试通过（Vitest）
- [ ] 并发测试无数据竞争
- [ ] RLS 测试确认用户数据完全隔离
- [ ] 安全扫描无高危漏洞
- [ ] 性能测试满足 <500ms 响应时间

#### 手动验证清单
1. 运行 `pnpm test:coverage` 查看覆盖率报告
2. 使用 k6 模拟 100 并发请求
3. 用不同用户账号尝试访问他人数据
4. 运行 `pnpm audit` 检查依赖漏洞
5. 使用 Lighthouse 测试性能得分

---

## 四、风险缓解策略
（列出每个阶段可能的风险和应对方案）

## 五、回滚方案
（如果某阶段失败，如何安全回退）

## 六、成功指标
（项目完成后的关键指标）

## 七、时间估算
（每个阶段的预计工作量）
```

---

## 关键约束

1. **不要编写任何实际代码** - 只需要详细的执行计划
2. **必须考虑成本优化** - 每个模块都要说明如何降低 AI API 成本
3. **必须包含安全考量** - 每个阶段都要有 RLS 和权限检查
4. **必须可测试** - 每个交付物都要有明确的测试方法
5. **必须向后兼容** - 不能破坏现有功能，要支持灰度发布

---

## 输出要求

1. 将计划保存为 `/graylumAI_vercel/movetonew/GraylumAI_分阶段重构执行计划.md`
2. 使用清晰的 Markdown 格式（支持折叠章节）
3. 每个清单项都要可勾选（`- [ ]`）
4. 技术术语使用英文，描述使用中文
5. 必须包含具体的文件路径和函数签名示例
6. 必须说明"为什么这样做"而不仅仅是"做什么"

---

## 参考资料

- 已上传简报：`AI_REFACTOR_DESIGN_BRIEF.md`
- 技术栈文档：
  - tRPC: https://trpc.io
  - Drizzle ORM: https://orm.drizzle.team
  - Claude API: https://docs.anthropic.com
  - Supabase RLS: https://supabase.com/docs/guides/auth/row-level-security

---

## 开始执行

请基于上述要求，制定一份完整、可执行、经过深思熟虑的重构计划。

计划制定完成后，我会逐阶段审核，确认无误后我们再开始实际编码。

现在开始制定计划。
