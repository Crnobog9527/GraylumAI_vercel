# 账号成果提案：产品文档同步建议

Owner 已于 2026-09-12 确认有界方案并授权实施；配合[最小方案](account-artifact-reuse-plan.md)。本文件记录批准事实及待同步内容，不表示四份受占用文件已更新。实施分支为 `codex/account-artifact-reuse-20260912`；实现和隔离验证进行中，未合并、未部署。

## 当前依赖与写入归属

2026-09-12 fetch 后 staging 为 `52041377fe564284f63b0d4e07c1cf624b500907`。live #388 `docs(v3): define chat-native Skill workflow and acceptance` 仍 OPEN，base `420b420ba25d1433ee8c9bbf8712a0ba04b1f6b3`，head `e8ff182bb84bdc5fd17b1b47d3f9ff2bc85aa9b0`。其分支 `docs/v3-chat-skill-spec-20260908` 占用下列四份文件；研究分支 `codex/account-artifact-plan-20260912` 只写两份 proposals，不覆盖其他工作或本地预览。

不只是文件重叠：#388 的 Codex review 为 COMMENTED，评论 [3953729384](https://github.com/Crnobog9527/GraylumAI_vercel/pull/388#discussion_r3953729384) 指出 Master Plan 历史审计优先级从 P0 改成 P1，与聊天内 Skill 规格范围无关。当前候选 diff 仍包含历史优先级改动；自动审查显示 Completed 不代表该问题已解决或候选 clean。本提案不认可这些无关历史改写，也不自行代其修复。

**四份文件写入保持阻塞，直到相关 writer 归属明确。** 由原归属处理审查问题或明确交接；未来同步重新读取 live refs、完整差异与审查，不沿用旧 PASS、不覆盖已合并内容。#388 合并不是本提案可以自动要求或执行的前提；不接管、合并、关闭或发送指令改变它。两份提案修订及复审可独立完成。

## 1. `docs/launch/Graylum_Master_Plan_v10.1.md`

写入归属明确后，在 V3 扩展处拟增加：

> Owner 已确认的有界扩展（实现与验收单独记录）：从用户本人可访问的明确正式定位报告发起同账号独立脚本，复用现有聊天、成果和报告阅读。仅补独立作品/修订身份、最小账号关联与一份经校验的固定版本引用配置；同账号同一脚本 Skill 支持 A/B 独立作品，保留原定位项目约束。见 proposals/account-artifact-reuse-plan.md。首批验证采用定位→YouTube 脚本，不表示任意跨 Skill 已支持，不新建账号中心或知识库，不改变首发必需项。

不改写历史审计严重度、冻结决定、旧任务图和费用规则。Owner 确认后可记录批准事实，仍需单独记录实现与验收状态。

## 2. `docs/launch/tasks/V3-standard-skills.md`

保留原阶段“不增加任意多项目、团队协作或跨 Skill 共享”的记录，另列 Owner 已确认的有界扩展，不将例外追溯写成旧阶段能力：

> 新提案仅允许明确正式定位→已配置脚本的引用闭环。新作品独立 project，同作品修订独立 round，保留定位项目约束；来源是用户本人可访问的正式成果。目标项目拥有自己的引用/证据身份，源证据 ID 不跨项目冒用；引用依赖撤销时按目标成果/依赖后继粒度限制内容，不承诺逐句净化。可信已派发结果仍按原请求规则结算，内容许可与费用收尾分开。详见 ../proposals/account-artifact-reuse-plan.md。

新增候选验收包括脚本 A/B 并行与 A 修订隔离、源/目标双向鉴权、固定版本、撤销时间窗及无重复生成/费用；不撤销原允许/拒绝和私有方法保护矩阵。缺少定位只影响该引用入口，不新增全站脚本门槛或无定位模式；不得写成 V3-M3 已通过。

## 3. `docs/launch/plan-core.md`

仅拟记录“已批准实施的有界扩展”，链接最小方案。唯一候选实施范围：已有正式定位→同账号独立脚本→确认保存与找回；内部顺序为项目身份/约束兼容、引用适配、复用组件接线、隔离验收。不是先建账号中心的 A/B/C/D 平台路线。

不自动添加任务 ID、ready 标记或首发依赖，不改变 REL-1。#388 的写入冲突和无关历史变更需原归属处理，不能把“合并 #388”写成无条件指令；后续按实际合流状态核验。

## 4. `docs/launch/START_HERE.md`

拟增加入口：账号成果保存与跨 Skill 引用研究（`proposals/account-artifact-reuse-plan.md`）。说明：Owner 已确认实施；样本为定位→YouTube，产品支持限于经配置闭环，不代表首发新增必需项，不是实现或验收证明。此文件内可直接查看[提案](account-artifact-reuse-plan.md)，正式同步时使用前述相对路径。

## 同步与本轮验收边界

未来四份文档同步需：writer 明确；精确候选完整审查；链接有效；新作品/修订、来源权限与账务、样本/能力/首发范围一致；无无关历史优先级修改；原任务图及验收矩阵保留。未做到之前不报告“四份文档已更新”。

当前批准仅覆盖有界实施、隔离验证与 PR 交付。实际测试结果需按最终候选记录；真实模型、远端迁移、部署、合并仍未授权。四份文档为 BLOCKED_WRITER，两份提案已记录批准事实；不声称 Master Plan/V3 同步完成。

实施与隔离验证记录：[正式定位到独立脚本交付](../../testing/account-artifact-reuse-20260912.md)。该记录与 PR 区分候选代码、运行证据和未授权远端操作。
