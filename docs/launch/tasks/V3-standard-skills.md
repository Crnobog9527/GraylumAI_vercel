# V3 标准 Skills、工作台与研究：规格及 V3.1 第一批

状态：Owner 已批准产品扩展；本窗口仅选择 `V3.1-LOAD`。本规格不是完成证明或外部操作授权。V3 原方案（2026-09-06）为产品输入，V3.1《Codex 源码借鉴与实施修订》为复用及首批交付修订；不提交真实商业方法原文。

## 产品目标与不可省略的语义

- 标准目录保存独立文件，元数据发现、显式绑定、完整入口、按需读取方法与模板；禁止将整包展平为单条 published_content 后冒充目录运行。管理员私有导入/导出，普通用户只取得允许的界面字段和自己的成果。
- 普通聊天按请求读取当前发布版本；工作台轮次固定 `revision + package hash + workflow schema + template hash`。新轮次默认继承旧包，升级明确展示方法变化并重新确认受影响成果。每次执行仍查实时权限、模块启停、Skill 归档、版本撤销；历史成果可读与私有包可执行分开。
- 六步保留需求确认、对标分析、定位、内容规划、运营、变现的业务顺序。以界面保存/确认取代斜杠命令。AI 候选、可编辑工作稿、不可变确认快照分开；版本冲突保留未保存输入，迟到 AI 只成为候选。
- 前序修改、采用新证据或更换包使当前及下游确认待复核，保留内容与旧快照；可沿用并重新确认，不强迫重生成。仅获取未采用的候选证据不使全部成果失效。
- 同账号一个项目，至多一个 active draft。v2 草稿不改变当前正式 v1；全步骤有效确认后，原子、幂等地产生唯一递增版本和正式报告，再切换当前执行指针。放弃草稿不改 v1；恢复旧方向通过新轮次，不覆盖历史。
- 报告先保存页面编辑，再验证六步最新确认；从同一快照确定性汇编需求背景、对标、定位、内容、运营与变现、来源/局限、版本说明。默认不调用模型；站内排版和 Markdown 导出忠实保留用户确认内容，重新下载不创建新版本。
- 策略差异绑定改变原因与当时证据；实践复盘绑定观察窗口和当时采用策略，没有实践资料只报告差异与证据不足。不同平台、窗口与指标分母不可假装可比。
- Skill 搜索/取数经全站受控服务端数据层，首选 AgentKey MCP + API Key，采用官方 SDK；不同时建设未经核实的 REST 路径。模型供应商搜索旁路在未来接线时关闭，普通非 Skill 聊天不默认迁移。
- 工具发现、执行前 schema/价格复核、允许能力、有限研究计划、预算、逐次调用与检查点由宿主控制；不向模型/浏览器开放任意工具、shell、URL 或凭证。结果未知先核实实际状态，不盲重试。首批不实现 MCP。
- 证据记录用户/项目归属、稳定对象身份、来源、获取/观察时间、样本/分页、缺失原因、指标口径、内容 hash 与保留条件；供应商返回、用户补充和修订分开。到期或删除传播到展示与导出，不以历史不可变绕过期限。
- 供应商成本与用户积分分开；取数和分析独立持久化/计量，分析失败不重新取数。复用原钱路与原子预留/结算，不建第二套余额。执行前校验权限/包/输入/预算；结果、费用与成果关联必须幂等且可恢复。用户价格、总预算及使用条款在外部启用前另行确认。
- 私有包正文/manifest/Key 不进公开接口、前端包、普通日志。模型看见方法后仍存在诱导复述风险，不承诺零泄露；密钥与权限裁决不交给模型。真实包须在未来权限修复与非生产验证后私有导入。

## 交付依赖与停止边界

| 节点 | 交付及完成要求 |
| --- | --- |
| V3.1-LOAD（本批） | 同步规格，两个虚构标准文档包经本地真实来源完成发现/激活/完整按需读取与拒绝测试；不切换线上聊天 |
| V3-PACKAGE-RESEARCH | 私有 DB 文件来源、完整包原子发布/撤销/权限；研究适配器及隔离测试；真实接口、价格/许可/额度、必要平台质量验证须单独授权 |
| V3-ARTIFACTS | 项目/轮次/成果/证据关联、保存/确认/失效/报告事务与跨用户拒绝、幂等恢复 |
| V3-WORKBENCH | 六步编辑与历史比较、共享 AI/费用适配；首次与后续迭代真实联通 |
| V3-M3 | 精确候选完整 staging 验收、内容接受、权限/供应商/成本/故障恢复及 Master Plan §7 全矩阵 |

依赖顺序见 [plan-core](../plan-core.md)。后续节点尚未由本窗口选择；这些是产品交付，不是 Gate、审计台账或自动执行引擎。历史 SKILL-1A/1B、BILL-1、PAY-1、CI-1 保留已合并事实，不能由此推导 V3 可发布。M3 无可复现 P0/P1、全部必须项有证据，RC 代码/schema/私有包/能力/配置固定后才算完成；仍不自动选择 REL-1。

本批禁止远端 Supabase/Stripe/Vercel/生产访问，禁止迁移与修改 0062/0063、配置 Key、收费调用、真实商业包、直接 push 保护分支、合并或部署。终点为一条 PR 的 Owner merge decision；现有 CI 自动运行，沿用 current AGENTS.md。

## 本批实现契约

`packages/api/src/services/skills/` 是独立 Node/TypeScript 薄核心，不从 root/router 或聊天运行路径导出。无新路由、空业务服务、ZIP UI、脚本执行或 Codex 子进程。

- `SkillSource` 分开 `list / state / read`。目录列出 immutable descriptor；本地 adapter 只接受受控注册根目录，未来 DB adapter 复用接口但必须自己实施真实授权、撤销与不可变读取。当前回调只是测试状态，不能作为 DB ACL 通过证据。
- descriptor 绑定 package/revision/directory、完整文件路径/字节数/媒体类型/SHA-256、资源依赖、可选任务资源计划和必需能力；确定性 package hash 包含这些信息。每个 revision 唯一，重复或 hash 漂移拒绝。hash 证明完整性，不证明来源可信。
- fixture 的 `catalog.json` 在标准包之外，是本测试宿主注册信息；不是 Agent Skills 标准文件，也不是内部商业 manifest 的公开接口。生产注册信息只能来自受控服务端。非多步骤包无需 `tasks` 内容或任何社媒六步扩展。
- 宿主明确选择已审阅的资源计划。入口总是完整读取；无条件 `requires` 和任务根资源的依赖闭包完整读取，其他 reference 不读。多任务包必须选合法 task，无任务包直接读取入口依赖。不会让模型猜文件或把 Markdown 链接当自动执行命令。**本批不承诺解析任意自然语言/Markdown 引用**：包接入者须提供与方法一致的资源依赖；未来导入/发布器必须检查该映射，不能未经审阅把第三方目录标为完整支持。
- 发现阶段在本地读取入口提取元数据，但不加载参考文件或将入口正文放入目录。激活重新读取并校验原字节。必需资源缺失、无权限、禁用/归档/撤销、未知 task/身份、越界、链接、脚本/二进制及未知必需能力均明确失败，无名称或版本猜测。
- 路径使用规范相对路径；拒绝 `..`、绝对路径、反斜杠、编码路径、等价大小写路径、符号/硬链接。本地根目录由操作者控制，不能接受用户上传可并发改写的树；哈希与读前/后检查不等于通用敌对文件系统沙箱。
- 包 ≤64 文件、展开总字节 ≤2 MiB、最多64任务；目录 ≤256版本。方法容量按 UTF-8 字节及最终 JSON 包装计算，任何不足明确失败，不截断。未来模型适配器必须在扣除其他上下文/输出预算后按具体 tokenizer 再校验；本批没有模型 token 容量证明。
- `LoadedSkill.forModel()` 是唯一显式取得私有上下文的方法；它不是用户响应。默认 JSON/普通 inspect 仅投影 packageId/revisionId/name/description；内部资源标识通过独立方法取得。错误只含固定代码，不保留原始 YAML/文件错误 cause，不记录正文。公开名称与描述必须是可公开元数据，不能放商业原文。
- 不缓存用户授权。发现后仍重新检查执行状态，逐资源读取与最终返回前检查撤销；未来实际模型/收费取数入口必须再次检查当前授权，不能将这个本地结果当永久执行令牌。

## 标准校验与宿主兼容分开

以 [Agent Skills specification](https://agentskills.io/specification) 和其 [宿主指南](https://agentskills.io/client-implementation/adding-skills-support) 为格式参考；指南的宽松建议不覆盖严格发布约束。

| 输入 | 标准/本宿主处理 |
| --- | --- |
| 合法名称、描述、块标量、带冒号的合法 YAML | 成熟 `yaml` 库解析；两个真实样本通过 |
| 缺 name、名称/目录不符、超长描述、重复键或非法 YAML | 严格拒绝；返回固定诊断，不修复或重写原始待发布字节 |
| YAML alias | 本宿主不展开 alias；格式报告为未评估（null），兼容报告为不支持，不冒称严格格式已通过 |
| 未知可选元数据 | 原文保留，不因不认识而删除，也不因此授予能力 |
| `allowed-tools` 或环境要求 | 可满足格式要求，但本宿主不支持工具且不能自动确认环境，单独报告兼容问题 |
| 未支持的必需能力、脚本/二进制、非规范路径 | 宿主拒绝；并非声称 Agent Skills 标准禁止所有脚本 |

直接复用：`yaml@2.9.0`（ISC，含 TypeScript 声明）和项目已有 Zod/Node API。锁文件中 Vite/Vitest 的 YAML 可选 peer 后缀是此次依赖解析结果，未升级它们。开工核对 #330 等 Dependabot 的版本升级与本批添加 YAML 的语义范围，未关闭、合并或改写其他 PR。

借鉴行为：固定 [openai/codex@6af345407d9c2a568da9d01b6c4b81a9e61495c0](https://github.com/openai/codex/tree/6af345407d9c2a568da9d01b6c4b81a9e61495c0) 中 `codex-rs/skills/src/parser.rs`、`parser_tests.rs`、`selection.rs`、`selection_tests.rs`（前170行）、`loading.rs`，以及 `codex-rs/ext/skills/src/catalog_prompt.rs`。参考其解析诊断、明确身份、禁用拒绝、来源分离与完整按需读取。未复制/翻译 Rust 实现或原始样本正文；TypeScript 代码与虚构样本独立编写，**不是 Codex 原生 runtime**。该固定仓库的 LICENSE 为 Apache-2.0；未来复制/移植具体内容时须保留适用许可、归属与修改声明。

Codex parser 的缺名回退、非法标量修复、超长描述兼容不用于 Graylum 严格发布。Codex 测试为阅读参考，未构建或运行其套件；官方 skills-ref 也未执行。

## 验证与不能推导的结论

真实 fixture：`workshop-notes`（gather/outline 两任务，v1/v2 验证固定版本）及 `reading-note`（无工作台扩展）。测试对完整字节与末尾哨兵断言、记录真实读取路径，并用独立临时副本验证缺失/篡改/链接/UTF-8/容量等拒绝；不以 Mock 替代两包文件读取。

命令：`pnpm test:api`，定点 `pnpm --filter @repo/api exec vitest run src/services/__tests__/standardSkillLoader.test.ts`，以及 `pnpm --filter web lint`、`pnpm --filter web typecheck`、`pnpm build`、`git diff --check`。新测试匹配 API 的 `src/**/*.test.ts`，required Unit Tests 已调用 `pnpm test:api`；新核心显式加入 web tsconfig，现有两项 required TypeScript 检查覆盖它，不更改 workflow。最终执行结果及 exact candidate 以 PR/CI 证据为准，规格不预填 CI PASS。

本批未实现：DB 文件来源/权限修复/原子包发布、工作台与成果落库、AgentKey、模型上下文适配/效果、费用适配、导入 UI。**NOT_RUN**：远端 DB/RLS、真实发布/撤销、模型效果、真实数据质量/价格/条款、费用/支付/退款、M3 产品流程、生产及部署。格式和文件加载 PASS 不能替代其中任何一项。

## 后续批次：V3-PACKAGE-RESEARCH（仓库与隔离验证）

2026-09-06 Owner 在 V3.1-LOAD 合并后选择本节点的仓库实现与隔离测试。上文“本窗口/本批”保留为 V3.1-LOAD 历史范围。新增私有目录 DB 来源、完整发布/撤销/权限、受控研究适配器与隔离运行证据见 [本批说明](V3-package-research-validation.md)。真实平台能力、价格/许可、远端权限/部署与费用验证仍为 NOT_RUN，不能据此把整个节点标完成或解锁后续任务。
