# #413 双 Skill 验证切片：实施中

本文件记录局部运行证据，不是交付通过或新的执行流程。

## 范围与当前归属

Owner 已明确批准双 Skill、成果承接与最小确认偏好的代码、隔离测试和独立 PR 交付。启动事实记录于 [Issue #413 评论](https://github.com/Crnobog9527/GraylumAI_vercel/issues/413#issuecomment-5645994467)。原 Issue 的提案状态不代表后续授权不存在；长期愿景仍不属于本切片。

- staging 核验基准：`52041377fe564284f63b0d4e07c1cf624b500907`。
- 已提交依赖 #412：`98e29eef3b8e4aee9fb9e217fec2d26926f1b010`。
- 分支：`codex/dual-skill-slice-413-20260912`。唯一 writer 为当前任务。
- #412 未提交的导航、工作台与对话 UI 修改独立保留，未导入本分支。临时目录清空后，已从本任务编辑记录及原组件提交恢复到持久 worktree；这不代表其界面已经重新验收。不得用旧 #412 的 CI/审查替代本候选验证。
- #388 四份规划文件未修改，正式规划同步尚未完成。
- 风险 high：SDK 依赖、权限与 SQL，以及后续计量接线。没有真实模型调用、远端数据库变更、显式部署或合并。

## 已运行的局部验证

| 验证 | 结果 | 实际范围 |
|---|---|---|
| 官方 SDK 包 | 核验 | 固定 `@openai/agents@0.18.0`、`openai@7.15.0`；读取已安装源码与类型，普通 Agent/Runner，不是托管 Agents API |
| `vitest run src/services/agentSlice/runner.test.ts src/services/agentSlice/artifactReader.test.ts` | 14 PASS | 真实 SDK，虚构 transport；两次模型调用、固定读取、截断、缺 usage、不重试、写入确认丢失、不同 Skill 指令隔离、停止、关闭 tracing、固定成果版本及拒绝 |
| `node packages/db/tests/v3/run-local.mjs --agent-slice-only` | 3 PASS | 新建可销毁 PostgreSQL/PostgREST；重复应用迁移；偏好确认、重放、纠正、删除、并发写、跨用户/撤销/删除身份拒绝、角色权限 |
| TypeScript | PASS（偏好路由阶段） | 后续改动须再检查，不代表网页/API 闭环 |

SQL 测试的 Auth `getUser` 为虚构身份边界，之后的 HTTP/RPC、事务和角色拒绝真实运行。未连接 Owner 的既有本地预览数据库。测试完成后仅清理本次创建的隔离容器。

偏好纠正和删除后旧正文不保存在重放记录中；保留无正文的版本墓碑，供刷新后的重新设置使用。执行引用只保存偏好身份/版本，解析时必须仍有效。单作用域至多 20 条有效偏好。新路由尚未接入用户页面。

成果读取工具仅提供固定正式成果的选定章节；模型不能传任意项目 ID。0082 为目标轮次建立本地证据身份，保留固定来源版本、章节配置及依赖；不复制另一份报告正文。递归检查按证据身份防环，最多八层承接。此界限是本切片的明确限制，不是通用引用平台。

2026-09-12 中断恢复后，真实本地 Auth、HTTP/PostgREST、隔离 PostgreSQL 的 `run-workbench.mjs --agent-slice-only` 已运行：1 PASS、152 SKIPPED，重复应用迁移通过，私有 canary 未出现在应用日志。覆盖脚本 A v1 → 标题 v1 → A v2、B 独立、创建承接重放、跨用户拒绝、源证据撤销后的依赖隐藏；0083 同一 conversation 两个不同执行固定各自项目/轮次/方法版本，相同请求更换目标被拒绝。未派发模型，不能据此声称费用或正式页面接线通过。恢复后的 SDK 12 项测试及 TypeScript 也重新通过。

后续来源与请求的历史、最终结果及恢复出口仍须完整接线验证。0083 目前仅登记执行身份，用户输入复用原 artifact_requests；不能将登记成功当作 SDK 执行完成。

SDK 当前协议边界为非流式 provider completion；未将此验证描述为已实现网页流式。`beforeCall`/`recordCall` 已通过 `sliceAccounting` 接入原消费检查、实际请求容量/报价计算和持久事务。失败的持久写不在 SDK 中盲重放；恢复先读回原调用身份，仅已知 responded 结果执行结算。

0084 新增单次模型调用身份与状态，复用 `atomic_pre_deduct`、`atomic_settle`、`atomic_refund` 及现有消费、用量表，不保存提示词或回答正文。隔离事务测试已验证：登记重放不增加预扣、派发权只能取得一次、unknown 不盲退款或结算、缺失 usage 拒绝、来源撤销后可信截断用量仍幂等结算。虚构余额 100 → 93，预扣、消费和用量记录各一次。该轮为 1 PASS、152 SKIPPED，迁移重复应用与日志 canary 检查通过。初次测试因样本未开启 AI 准入、随后因虚构余额为零而拒绝；修正仅限可销毁测试样本，没有修改远端设置。

后续 SDK 接线测试使用真实 Agent/Runner、真实本地 Auth/HTTP/SQL 与可计数供应商替身：读取固定 A1 成果的两次模型调用分别结算，原执行重跑不增加供应商调用；缺少 usage 的回应保存 providerId/finishReason 与空用量，保持 unknown，不写成功消费/用量记录。报价按 SDK 实际请求内容计算容量，包含工具定义和工具结果；只保存内容哈希，不保存私有请求正文。未知或超容量用量不转换成零费用成功。

0085 增量保留旧执行/调用身份，为新执行固定后台现有配置中的独立整理模型及输出上限。回复至多两次调用（含受控读取），整理无工具、至多一次；两阶段分别预扣/结算，但合计预留受同一固定预算约束。没有有效且已保存的回复时拒绝启动整理。旧执行未固定整理模型，不自动补猜当前模型。

0086 复用 artifact_candidates 保存阶段正文，artifact_requests 只关联执行/阶段/证据，不复制另一份回答或报告。保存须经过现有完整输出检查及私有方法回显检查；未知或截断结果不能保存为成功。读取重新校验身份、私有方法与来源。回复不能采用为步骤成果；整理采用保留原候选保存、版本冲突和同请求幂等，步骤改变后不能用新的采用请求覆盖。阶段存储不是公开原始正文写入口。

2026-09-12 本轮局部测试已运行：SDK/固定读取 13 PASS，TypeScript PASS；真实 SDK + 本地 Auth/HTTP/SQL 的阶段身份及成果保存 1 PASS、152 SKIPPED。回复两次、整理一次用量记录独立；恢复不增加模型调用；私有 canary 保存拒绝、来源撤销后的回复/整理读取 restricted、跨用户读取拒绝。迁移重复应用与日志 canary 检查通过。故障注入中整理阶段的结算接口暂时不可用：已保存完整正文仍可读取，调用保留 responded；恢复后仅结算原调用，供应商计数不变。采用同请求重放成功，新请求不能用旧整理结果覆盖已变化步骤。尚未完成正式网页、私有方法装配与端到端延迟验证。

0087 与 loadSliceContext 已接入实际 databaseSkillSource/activateSkill，按执行固定的方法版本和当前步骤资源加载唯一方法。上下文只含必要步骤与已确认偏好，当前步骤正文不重复放入祖先列表；后续对话历史与固定成果工具装配仍待完成。上下文 RPC 不向前端开放。

局部隔离验证（private-context-preferences.log）：1 PASS、152 SKIPPED，重复迁移与日志 canary 检查通过，TypeScript PASS。标题执行经实际加载器取得 BETA、返回 A 修订取得 ALPHA，互不包含对方方法文件；真实 SDK 测试使用加载出的标题方法。偏好确认后装配原值，纠正后旧执行拒绝、新执行取得新值，删除后该执行拒绝；跨用户上下文读取拒绝。这不是新聊天网页或完整双 Skill 用户流程通过。

执行服务 sliceExecutor 已把固定方法装配、固定成果读取、真实 SDK Agent/Runner、逐次账务及阶段结果保存串联。回复必须实际读取一次所选成果；整理不开放工具。读取仅输出配置允许的章节和版本，不向模型提供目标证据 ID。新 tRPC executePhase 只接受已登记执行 ID 与阶段，result 是无结算副作用的读取；新执行准入已有后续服务与 API 接线，页面仍待完成。

服务级串联测试已通过：实际私有方法加载，回复两次调用、整理一次，两个完成阶段重进均直接读回正文，未增加调用。14 项 SDK/读取单测通过。首次 HTTP 测试用 Bearer token 返回 401，按网站现有 Cookie 登录方式改测；修正测试脚本编辑错误后再验证。最终 executor-browser-final.log：1 PASS、152 SKIPPED，浏览器真实登录后，正式 tRPC 读取及已完成阶段重试均返回保存正文，供应商计数保持三次。重复迁移、日志 canary 和修正后的 TypeScript 检查通过。尚未验证由网页新建并执行双 Skill。

发送准入 sliceAdmission/0089 已加入正式 begin API。服务器读取既有主模型和独立整理模型，按既有价格与预留算法计算两次回复调用加一次整理调用的固定预算上限；这里只登记，不扣费，实际各次调用仍由原原子预扣/结算执行。模型与费用配置不是前端输入。原请求重试先校验原执行与原消息身份，再返回原登记结果，不因后续配置变化变成新执行；新输入复用旧 requestId 被拒绝。提交时在 SQL 内核对整理模型配置，避免预查与登记之间静默更换模型。

admission.log 局部隔离验证：1 PASS、152 SKIPPED；实际准入→SDK 两阶段→保存，以及浏览器 Cookie 登录后的 begin 重试/结果读取/已完成阶段重试通过。模型配置改变后的原准入重试仍返回原身份，后续恢复配置仅发生在可销毁测试库。TypeScript、重复迁移、日志 canary 检查通过。

独立 recover API 已实现，只处理确定的原调用身份与可信 responded 用量，不加载模型凭证或方法/来源正文，不重放模型、不处理未知结果退款。纯 result 查询仍不触发结算。admission-recovery.log：1 PASS、152 SKIPPED，重复迁移与日志 canary 通过。验证 responded 恢复结算、来源撤销后的原调用恢复且供应商计数不变；不代表全部故障时间窗与页面刷新通过。

0090 提供同一 conversation 的有界记录投影，复用原输入和阶段候选。最新 20 条、时间戳与请求 ID 双字段游标；纯读取不生成、不结算，来源撤销同时隐藏该轮输入、回复和整理正文，跨用户拒绝。读取异常不冒充空历史。

正式 `/chat?mode=agent-slice&conversation=...` 已接入共享 AppHeader/ChatSidebar 与此投影；这是尚未完成的正式路径，不是独立模拟页面。当前支持历史显示、继续已有目标、轮内阶段提示、阶段结果直接显示和原执行重试。正文暂为安全纯文本，尚未完成 Markdown/代码体验；没有供应商增量流或 SSE 实时事件，不宣称流式已交付。首次选择/创建作品、配置的双 Skill 选择、偏好编辑、模型完整历史、来源选择与成果确认侧栏仍需接线。不得用这个阶段的路由冒充完整 Owner 验收入口。

conversation-pagination-final.log：本地 SQL/Auth/Next/浏览器集成 1 PASS、152 SKIPPED，重复迁移与日志 canary 检查通过；14 个 SDK/读取单测、TypeScript 通过。浏览器真实登录后打开正式聊天路由、读取已保存正文并刷新，供应商计数保持 3；同会话含 A 和标题的不同执行，跨用户读取拒绝，撤销来源后正文消失。三条完全相同时间戳的记录分两页读取无重复/漏条。没有运行真实供应商。首轮新增测试将 A 的断言放在 A 执行创建之前而失败，已移动到创建之后验证；未改业务逻辑绕过断言。

0091 增加专用会话模式与幂等打开：新入口 `/chat?mode=agent-slice` 创建后使用普通 conversation URL 找回。模式固定，原有会话默认保持旧模式；配置只列本人已有的当前草稿及脚本/标题用途，不返回方法文件。新请求不能把普通会话偷偷交给 SDK；普通 SSE/旧 AI 生成入口拒绝该专用模式，原请求恢复仍在新消费检查之前。此处是同范围执行隔离，未改报价或收费规则。

正式页面可选择已有脚本/标题作品及步骤，并显式保存、纠正、删除用户或当前账号范围的写作偏好。新发送重新取得当前有效偏好引用；只有 Owner/用户确认的信息才会保存。首次从定位新建 A/B 与正式成果承接侧栏仍未在这个页面闭环，不能据此声称完整双 Skill 产品通过。

entry-browser-send-final.log：1 PASS、152 SKIPPED，重复迁移、日志 canary、TypeScript 通过。真实浏览器 Cookie 登录，在正式 `/chat?conversation=...` 页面选择标题作品并发送，经真实 Next/tRPC/SDK/本地 Auth/SQL；仅在可销毁源副本将供应商传输替换为计数 loopback HTTP。实际两次 reply 调用含一次受控成果读取、一次独立 summary，三次分别结算；刷新后仍为三次。供应商替身验证纠正后的偏好存在、旧值不存在；页面确认/纠正/删除均通过。新会话打开重试返回同一身份，配置选项含独立 B，旧普通 SSE 返回 403 且没有生成。首两轮边界测试分别缺少 Bearer 和隔离库普通请求迁移，分别得到 401/503；补齐测试认证/迁移，并将新请求模式检查前置到消费准备之前后通过，未放松安全检查。

entry-ordinary-regression.log：3 PASS、150 SKIPPED，重复迁移与日志 canary 检查通过。原自由/文档流式发送、URL 与中断身份保持，以及真实用量/缺失用量处理不回归。该三项不覆盖全部旧路径与导出。

## 成果承接应用闭环（0092）

0092 将创建作品和固定跨 Skill 来源绑定放在同一事务，复用原项目、轮次、候选、确认和正式报告，不新增正文副本。脚本到标题创建独立作品；标题带回明确选定的脚本正式轮次时，为该作品开启修订。当前聊天保留所有轮次；侧栏采用、编辑、确认、发布与报告阅读使用正式 workbench API。正式报告在宽阅读弹窗中显示，来源版本在作品侧栏可见。

artifact-browser-loop-final.log：本地 SQL/Auth/Next/真实浏览器集成 1 PASS、152 SKIPPED，耗时 83.97 秒；迁移重复应用及日志私有 canary 检查通过。浏览器发送脚本 A、采用并发布，按固定 A 生成标题，编辑选定标题并发布，再带回 A 开启新轮次、发送、采用并发布 v2；刷新后这些消息仍可见，独立 B 未改变。计数替身包含前置偏好验证在内共 12 次实际 SDK HTTP 调用，刷新不新增调用。首次 A 创建当前仍由正式 API 测试准备完成，并非用户入口已验收。全部为虚构方法、测试身份和本地供应商替身，无真实模型或费用。

该轮修复了事务中的同名变量歧义，以及整理正文与旧正文相同时错误隐藏采用按钮的问题。TypeScript 通过。以上是局部闭环证据，未完成流式、首次创建入口、完整回归或精确候选审查。

## 从正式报告创建独立 A/B（0093）

正式聊天增加“基于定位报告新建脚本”：服务端只列本人有权限且配置匹配的正式定位版本元数据；用户明确选版本与名称，同请求重试返回同一作品，再次明确新建产生独立项目。无匹配报告只影响此入口；读取失败显示错误，不冒充缺失。采用现有来源和创建事务，无新模型调用或正文副本。

source-entry-browser-fix.log：隔离 SQL/Auth/Next/浏览器 1 PASS、152 SKIPPED，88.24 秒。现在 A/B 均由真实页面创建，验证并行草稿、A 创建请求重放、列表/创建不增加模型调用，随后完整执行 A→标题→选题修改→A v2→刷新，B 工作稿保持空白。12 次计数 SDK HTTP 调用包含前置偏好验证，刷新不追加。重复迁移及日志 canary 检查通过；TypeScript 通过。第一轮在标题切换处捕获短暂选项错位，已保留尚未进入查询缓存的选中身份，并加强测试核对标题作品后通过。

定向 ESLint 命令退出 0，但仓库当前配置将上述 TSX 全部忽略，因此该项记为 SKIPPED，不是语义或 lint PASS。新增来源列表的进一步权限/撤销及故障矩阵仍需补充，不声明完整验收或 candidate clean。

## 创建与来源异常边界

source-boundaries-fixed.log：1 PASS、152 SKIPPED，84.93 秒，包含此前完整浏览器 A/B→标题→修订→刷新路径。新增验证：来源列表仅元数据、不含 POSITION 正文；另一测试用户列表为空且创建被拒绝；普通 authenticated 角色不能直接调用私有来源 RPC；非法标题→标题绑定在创建后失败，项目、轮次和引用均回滚为零；移除账号关联后列表为空、创建被拒绝且没有留下项目。0093 来源列表补充账号归属条件，避免展示已经不能采用的报告。

TypeScript、重复迁移与日志 canary 通过。首次新增测试引用了不存在的 fixture.skillId，类型检查发现后中止该次测试，改为 fixture.pack.id；已清理中止运行产生的专用临时容器与副本，再运行上述通过结果。未修改真实账号或远端数据。该证据不代替全部撤销时间窗、进程退出和权限回归。

## 偏好跨聊天与浏览器观测

preference-new-chat.log：1 PASS、152 SKIPPED，84.70 秒；TypeScript、重复迁移及日志 canary 通过。浏览器在已有聊天确认“先给具体例子”，纠正为“结尾给行动建议”，随后进入不同 conversation 发送。三次 reply/summary 实际替身请求均包含新值、不含旧值。删除后继续 A→标题→A 修订的九次请求，两条偏好文本均不再进入请求。完整 A/B、回滚和权限边界同轮复跑通过。

同一轮在本地 Apple Silicon / Node 24 / Next 开发环境，以浏览器 submit 事件和 DOM MutationObserver 观测：助手位置首次反馈 16.7ms，完整安全回复显示 1763.3ms。是单次虚构供应商观测，包含本地 HTTP、开发环境开销及替身等待，未分离真实网络或真实供应商生成耗时；不是总速度承诺，也不是首个安全增量流延迟。最终提交到页面完成的独立测量仍未完成。

API 回归（full-api-regression.log）：在不加载本地凭证的 test 环境运行 pnpm test:api，79 个测试文件、1822 项通过。此项为 API 单元/模拟回归，不代替隔离 SQL 或远端 CI。

## 尚未完成，不能计为通过

- 同一 conversation 的不可变执行绑定已完成局部 SQL 验证；A/B 创建与修订应用已有局部通过，完整故障矩阵尚未完成。
- 固定来源、目标证据和依赖限制在所有读写/历史/恢复出口的衔接。
- SDK 逐次调用的服务/SQL 接线已有局部验证；正式入口、阶段自动装配及完整恢复尚未完成，不能计为可交付闭环。
- 正式聊天页面已有发送、保存与刷新局部证据；SSE 轮内实时反馈、Markdown 和迟到结果展示防护仍需完成。
- 用户偏好跨聊天及纠正/删除已有浏览器证明；账号范围、并发和迟到写入的完整应用验证仍需完成。
- 实际 Auth/正式页面/API/数据库全流程、进程中止、并发、来源撤销时间窗、延迟测量。
- 精确候选 CI/Security、独立完整 Codex 审查及 PR 交付。

原 M3/REL-1 和既有产品验收不变。真实非 GPT 模型配置及质量/费用验收未运行；虚构协议通过不证明真实模型兼容。当前候选不 clean，不请求合并。

## 2026-09-13 独立审查后的修复（未交付）

独立 Codex `/root/slice_full_review` 完整审查 base `52041377fe564284f63b0d4e07c1cf624b500907` → head `c2a6173a5c89dd76ef02694bee1c183b25c8ff68`，结论 CHANGES REQUESTED；报告已记录在 PR #414 评论 `5646987054`。四项为正文持久化失败后的不可恢复、后台结算缺少正式入口、未采用讨论未进入后轮、A/B 切换后旧报告弹出。来源 Skill 撤销假设经原历史读取政策核对后撤回，未据此扩大权限限制。

本次局部修复：
- 报告及承接动作绑定原项目/轮次，迟到报告不在另一作品下弹出；草稿仍按轮次/步骤保留。
- 0094 仅固定已保存回复的身份，正文继续从原输入/候选读取。限定同 conversation、同作品/轮次，最多八轮、64,000 字符；最新完整轮优先，模型容量仍在每次真实请求中校验。只采用来源已包含在本次证据中的讨论，来源失效读取失败，不作为空历史。偏好版本不同的旧生成讨论不注入；跨作品仍通过明确选定的正式成果承接，不混入 B 的讨论。
- 正式聊天页面加载/刷新后独立调用原结算恢复，先展示成果，不让用户确认费用。新一轮工具调用前可补结算前一次已知 responded 调用；未知结果不重派、不退款。持续结算故障和进程退出窗口仍需进一步处理。

运行证据：`late-report-repair.log` 1 PASS / 152 SKIPPED；`discussion-maintenance-final.log` 1 PASS / 152 SKIPPED，真实 SQL/Auth/HTTP/浏览器 + 真实 SDK/虚构供应商。验证未采用回复进入实际模型请求、B 不混入、旧执行历史引用不变化；正式浏览器刷新将待结算整理变成 settled，仍仅原两次 reply/一次 summary；A/B、标题、A v2、偏好纠正删除与来源撤销回归通过。迁移重复应用及日志 canary 检查通过。此前两次重测在浏览器作品切换的默认短等待超时，调整为明确 10 秒有界等待后通过，并未改变供应商调用与身份断言。

旧候选完整 workbench 回归：112 PASS / 1 FAIL / 40 SKIPPED，失败为 SLICE 样本初始化读取全局 catalog，前面累积测试注册后触发已有目录上限；改用现有按模块 catalog 的 start 参数。完整回归与重启阶段须重新运行，不能写为全套 PASS。TypeScript 已通过修复阶段检查；精确新候选 CI/独立复审未运行。正文持久化 P1、完整状态恢复、最终运行验收和其他已声明限制仍未关闭。

### 正文提交与有界失败恢复

0095 将可信用量证据与完整检查后的正文一起提交；正文仍只有原 artifact_candidates 一份。原供应商响应的最终 assistant 文本在 SDK 返回前进入该事务。提交响应不确定时先读取原阶段及调用身份，已提交即直接找回；确认尚处 dispatched 且没有正文后，仅重试相同数据库提交一次，不调用模型。权限撤销阻止正文保存，但不抹掉可信用量；安全检查拒绝正文同样保留原用量收尾。

0096 为未能持久化的结果设置明确状态：派发两分钟后仍无可读结果时，显示 unavailable（分别保留 outcome_unknown / result_unavailable），不继续声称生成中、不自动重新派发、不盲退款。新调用按实际派发时间计时，不能用旧 prepared 的创建时间误判。它不是“恢复所有进程崩溃的正文”：如果服务在任何持久写之前退出，正文仍无法凭空找回，已知账务按原身份收尾，未知账务继续保持未知。页面可以继续新消息，原调用不会被当成新调用重试。

`final-commit-recovery.log`：1 PASS / 152 SKIPPED，提交前候选 INSERT 故障导致用量/正文一起回滚，读回后同数据库提交恢复；提交成功但确认响应丢失可找回；重新创建执行服务后仍取原正文。回复两次、整理一次，未增加模型调用。SDK/读取单测再次 14 PASS，TypeScript PASS。`bounded-final-recovery.log` 的同范围测试亦已通过测试主体，增加未知调用到期后 execute 直接返回状态、模型次数不变；进程清理结果需终态核对。精确新候选独立复审及完整回归仍未通过，不能标为 clean。

## Independent review follow-up: prepared recovery and revision isolation

Independent full review of `52041377fe564284f63b0d4e07c1cf624b500907` → `4bcebb5ab9dfe1dc46c61e9f25795ac02f17529f` returned CHANGES REQUESTED with two P2 findings. All required checks passed for that head; this does not validate subsequent changes.

- Additive 0097 handles expired, provably undispatched reservations through the existing financial recovery mutation. The original execution lock serializes recovery against dispatch; the existing atomic refund is reused. It does not refund dispatched/unknown calls or start another generation. Recovery requires an active authenticated owner and does not load restricted method/source content. No autonomous background worker is added; recovery runs when the application requests original-state maintenance.
- Additive 0098 handles only the existing exact `P0001 / Skill unavailable` outcome per conversation/list item. Other database errors continue to fail the read. Existing source/report permission semantics are preserved.
- `prepared-recovery-repair.log`: migrations and repeat application passed; new prepared-refund assertions passed before the suite failed because the fixture changed B's draft without restoring its expected empty state. This is a failed suite, not a pass. Fixture restoration and expanded dispatch-race/revision tests are now under verification in `prepared-and-revocation-repair.log`.
- Local TypeScript check passed before the latest test additions. Full regression on the preceding frozen head is still running. Fresh exact-candidate CI and full independent review remain required after these fixes. No real provider, remote migration, deployment or merge was run.

Follow-up result: `prepared-and-revocation-repair.log` — 1 PASS / 152 SKIPPED, 116.12 seconds, including the real application A→title→A revision flow, expired prepared recovery after saved-context change, replay without extra refund, cross-user recovery denial, concurrent dispatch/recovery, and per-revision history/target/source isolation. Additive migrations applied twice; private canary absent from application logs. This is the bounded slice suite, not all workbench regressions or real-provider validation.

## Further full-review repairs

Independent full review of base `52041377fe564284f63b0d4e07c1cf624b500907` → `91815e667a8c670c24bcb31c45ad996cd1254089` returned CHANGES REQUESTED: deterministic result rejection rolled back known usage, and the original report reference-choice list still mishandled a revoked target revision.

0099 keeps trusted usage in the original call when the result exceeds its step limit or candidate capacity; it records only a rejection flag, no rejected body, and exposes an unavailable result immediately. Existing atomic settlement remains the only charging path. 0100 handles only the known unavailable-revision exception per original reference choice.

`rejected-result-usage.log`: 1 PASS / 152 SKIPPED, 132.09 seconds; normal reply plus an over-limit summary produces exactly three SDK calls, three settled usage records, no summary candidate, and replay makes no extra call. The healthy original reference choice remains after the other method is revoked. Other slice regressions, migration repeat application and private-log canary passed; process exit 0.

The preceding full workbench run ended 112 PASS / 1 FAIL / 40 SKIPPED: the slice reused the existing fixture account, correctly violating the single-positioning-project constraint. The slice now gets its own explicit synthetic account association; product constraints are unchanged. `full-workbench-isolated-account.log` is running on that repair with migrations through 0098. It does not cover subsequent 0099/0100; the bounded slice above covers those. No full-regression PASS is claimed yet.

Local Owner preview smoke: the existing isolated harness seeded a fresh synthetic positioning report and A/B drafts, without running the destructive revocation assertions in that preview instance. This is seeding, not another claimed suite pass. Formal `/login` and `/chat` were opened in Chromium, an actual message returned through the loopback SDK transport, the summary was adopted into the existing step textarea, and a fresh page read retained it. Screenshot review found an undefined `--accent-primary` token; the user bubble now uses the site's `--color-primary`. Browser computed background `rgb(255, 215, 0)` and the corrected screenshot were verified. Preview initially used 91815e6 application code plus this identical bubble correction; financial fixes 0099/0100 are covered by the separate SQL/browser suite, not claimed installed in that running preview.


## 最新验证状态（2026-09-13，尚未交付）

- `rollback-admission.log`：进程退出 0，1 PASS / 152 SKIPPED，106.97 秒。隔离 SQL 中关闭 pair 后，新执行和调用记录均为零；已派发原调用仍记录可信用量并幂等结算。随后恢复测试配置。迁移重复应用与日志 canary 通过。未修改远端配置。
- `full-workbench-isolated-account.log`：主阶段 113 PASS / 40 SKIPPED，1204.64 秒；重启阶段失败，因此整条命令退出 1。恢复页面未找到预期三份正式文档，日志显示 catalog/projects 批读取 HTTP 207；尚未证明数据丢失或确定具体 RPC 错误。另有两个管理员初始化测试在重启阶段重复运行而失败。工具现将重启阶段限定为原恢复用例，尚未重跑通过；不会据此消除成果恢复失败。
- `5cccba364dd3b73c0444fc435bdb2369f05ea1b4` 的全部必需 CI/Security 与独立完整 Codex 审查已通过。后续本地测试及工具改动未提交，不能继承该精确候选的审查结论。
- 最新隔离体验实例已经包含 0099/0100；正式登录、发送、采用整理成果、刷新后恢复工作稿以及用户气泡颜色通过浏览器验证。模型仍为可计数本地替身，不能声称真实供应商验收。

当前未关闭：重启恢复失败的根因和复验、最终提交到完成交付的独立延迟测量、最终候选复审及交付记录。此前各阶段的“尚未完成”描述是历史进度；本段只更新已经取得的新证据，不将其他缺口自动视为完成。


补充本地交付观测：最新 5cccba3 正式应用预览，Apple Silicon / Node 24 / Next 开发模式，真实浏览器 submit 与 DOM 观察；本轮反馈 16ms、整个 reply+summary 完成 4968ms、summary HTTP 响应到完成 UI 217ms。供应商为本地替身，包含网络与开发模式开销。响应抵达晚于服务器提交，不能把 217ms 写为精确数据库提交延迟；不承诺真实模型速度。原始观测保存在本任务私有验证目录 `owner-preview-current/graylum-workbench-evidence-tfPRVp/delivery-timing.json`。


### SDK 实际接管的职责与保留范围

当前 `runner.ts` 由普通 `Agent` 定义本次方法与唯一受控读取工具，由 `Runner.run` 完成模型响应、工具执行、工具结果回送及最终输出的有界循环；没有再写一套应用层工具循环。reply 最多两次模型请求，summary 单独一次，均按实际调用计量。应用保留权限、固定上下文、原始 usage 检查、私有输出检查和原账务，这是业务边界而非 SDK 自动提供的保证。

本切片没有删除旧执行器：普通聊天与已有 Skill 仍需兼容。新 conversation 模式拒绝旧生成入口，隔离测试检查无 `artifact_generations` 双写，SDK 回放不增加调用。本批能证明新路径直接使用 SDK 的工具循环，不能声称全仓执行机制已简化或代码量净减少，也不据此扩大迁移。当前 SDK provider 路径仍为完整检查后展示正文，未完成全量流式改造。


`focused-workbench-restart.log`：独立创建/确认/发布阶段 1 PASS / 152 SKIPPED（173.01 秒），实际终止并重新启动 Next 后恢复阶段 1 PASS / 152 SKIPPED（48.41 秒），整条命令退出 0。新密码登录、每份保存版本与每一步正文比较均通过，迁移重复应用与日志 canary 通过。这说明干净样本下重启可恢复；不替代大量累积样本下原 HTTP 207 的根因确认。全量复跑加入仅输出目录数量/错误和项目数量的本地诊断，不输出方法正文或凭证。
