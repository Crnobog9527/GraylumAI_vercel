---
description: 历史 legacy tracker 流程参考，不是执行入口
authority: non-authoritative
---

# Legacy do-next reference

This document is retained as non-authoritative / derived / historical procedural material. It must not be used as a fresh-window executable entry and cannot select or create the current task.

Current startup authority is the verified live repository identity and refs, the authoritative root `AGENTS.md`, the accepted `docs/governance/DEVELOPMENT_POLICY.md` blob/epoch, the dedicated GitHub Task Issue, and a separate explicit Owner receipt. Missing, stale, ambiguous, or conflicting evidence must fail closed.

The legacy tracker files may be read only as historical context when an already-authorized review explicitly requires them. They cannot originate a task, receipt, permission, state write, implementation entry, commit, merge, deployment, or external mutation.

No command in this document can start implementation or pass control to a commit flow. Follow the current repository-level policy and explicit gate receipt instead.
