---
authority: non-authoritative
status: historical_reference_only
---

# Legacy task-tracker reference

This file is retained for historical and compatibility context only. It is not an always-on authority and cannot act as a fresh-window executable entry.

The repository-level authority is the verified live `AGENTS.md`, the accepted `docs/governance/DEVELOPMENT_POLICY.md` blob and epoch, the current dedicated GitHub Task Issue, and a separate explicit Owner receipt. Missing or conflicting evidence must fail closed.

The retained tracker documents (`task.json`, `progress.md`, `findings.md`, and `task_plan.md`) are non-authoritative / derived / historical. They cannot select the current task, create a receipt, grant permission, write state, authorize a commit, authorize a merge, authorize deployment, or authorize external mutation.

Natural-language requests do not create executable tasks or authorization through this reference. Any legacy task ordering, status, synchronization, or state-writer guidance below the repository policy boundary is documentation only and must not be followed as a mutation path.
